#include <iostream>
#include <cstring>
using namespace std;
 
int main()
{
	char c[100];
	string str = "", beWord;
	cin >> c >> beWord;
	int len = strlen(c);
	for (int i = len-1; i >= 0; --i) {
		str += c[i];
	}
	if (str == beWord)
		cout << "YES";
	else
		cout << "NO";
	return 0;
 
}
